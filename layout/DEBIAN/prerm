#!/bin/sh
# Unload the daemon BEFORE the binary is deleted during uninstall
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    launchctl unload /Library/LaunchDaemons/c.eolnmsuk.corelliumdecoy.plist 2>/dev/null || true
fi
exit 0
